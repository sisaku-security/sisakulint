on:
  workflow_call:
    inputs:
      title:
        type: string
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log("${{ inputs.title }}");
